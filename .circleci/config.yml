version: 2.1
orbs:
  ruby: circleci/ruby@0.1.2
jobs:
  deploy:
    machine:
      enabled: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - db:f5:a7:bd:d9:90:1b:db:e6:8f:8a:f4:35:53:d1:95
      - run: ssh -p $SSH_PORT $USER_NAME@$HOST_NAME "/var/www/minagawa-portfolio/deploy-me.sh"
workflows:
  version: 2
  deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: ski-site